
>[!success] 最值问题
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "最值") or contains(tags,"值域")
SORT file.name
>```

>[!help] 分段函数
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "分段函数")
SORT file.name
>```


>[!success] 抽象函数
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "抽象函数") or contains(title,"抽象函数")
SORT file.name
>```

>[!help] 赋值法
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "赋值") 
SORT file.name
>```

>[!success] 基本不等式
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "基本不等式") or contains(tags,"基本不等式")
SORT file.name
>```

>[!help] 对称性
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "对称") or contains(tags,"对称")
SORT file.name
>```


>[!二次函数]
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "二次函数") or contains(tags,"二次函数")
SORT file.name
>```

>[!success] 恒成立有解问题
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "恒成立") or contains(tags,"有解")
SORT file.name
>```

>[!help] 诱导公式
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "诱导公式") or contains(title,"诱导公式")
SORT file.name
>```

>[!success]  三角函数的性质
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "三角函数") or contains(tags,"三角函数的性质")
SORT file.name
>```


>[!help] 二次不等式
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "二次不等式") or contains(tags,"二次不等式")
SORT file.name
>```

>[!success] 定义域
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "定义域") or contains(tags,"定义域")
SORT file.name
>```


>[!help] 五点作图
>```dataview
TABLE file.link as "文件"
FROM ""
WHERE contains(tags, "五点作图") or contains(tags,"五点作图")
SORT file.name
>```


